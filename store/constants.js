export const PREV_PAGE = "PREV_PAGE";
export const NEXT_PAGE = "NEXT_PAGE";

export const SEARCH_DATA = "SEARCH_DATA";

// Search Data will fetch initial data

//prev page won't fetch any data , just return relevent data --> will filter the array

//next page will check for a condition and then fetch data  --> if(condition){return data} else{ return ...state,data}
